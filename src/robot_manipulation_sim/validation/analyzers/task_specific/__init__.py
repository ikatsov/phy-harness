"""Task-specific analyzers (YAML ``type`` strings tied to a ``task_spec`` / rubric).

The robot-manipulation-task-loop skill may **add** new modules here and register their ``type`` in
``analyzers/__init__.py`` (``TASK_SPECIFIC_REGISTRY``). Do **not** modify ``../generic/`` from the skill.
"""
